# `pnpm audit` 需要 npm 官方审计端点，项目内覆盖全局镜像配置
registry=https://registry.npmjs.org/

# Force hoist Next.js runtime dependencies for standalone builds
public-hoist-pattern[]=@swc/*
public-hoist-pattern[]=@next/*
public-hoist-pattern[]=caniuse-lite
