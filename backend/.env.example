HC_DATA_DIR=./data
HC_BIND=0.0.0.0:8080
# CLI 访问的后端地址
HC_API_BASE=http://localhost:8080
# 管理员口令（必须 >=32 随机字符）；未设置则仅靠已有用户登录
# HC_DEV_TOKEN=please-set-a-strong-random-token-32chars-or-more
# JWT 密钥与 iss/aud（重启持久化需要显式设置）
HC_JWT_SECRET=please-set-a-strong-random-secret-32chars
HC_JWT_ISSUER=hypercraft-api
HC_JWT_AUDIENCE=hypercraft-clients
# 逗号分隔命令名（仅文件名）
HC_ALLOWED_COMMANDS=java.exe,node.exe
# 分号分隔绝对路径前缀
HC_ALLOWED_CWD_PREFIXES=C:/apps;D:/work
# CORS 允许的来源，逗号分隔；空或 "*" 表示允许所有来源
# HC_CORS_ORIGINS=https://a.example.com,https://b.example.com